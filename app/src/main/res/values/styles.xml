<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ImageView">
        <item name="android:layout_width">@dimen/imageview_width</item>
        <item name="android:layout_height">@dimen/imageview_height</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_margin">@dimen/imageview_margin_sixteen_dp</item>
    </style>

    <style name="ListLinearLayout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:orientation">vertical</item>
        <item name="android:layout_marginLeft">@dimen/list_linearlayout_margin_sixteen_dp</item>
        <item name="android:layout_marginRight">@dimen/list_linearlayout_margin_sixteen_dp</item>
        <item name="android:layout_marginTop">@dimen/list_linearlayout_margin_sixteen_dp</item>
    </style>

    <style name="TextViewSameWidthHeight">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="View">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_marginTop">@dimen/list_linearlayout_margin_sixteen_dp</item>
        <item name="android:background">@android:color/darker_gray</item>

    </style>

    <style name="IconLinearlayout">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="MatchWidHeight">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

</resources>
